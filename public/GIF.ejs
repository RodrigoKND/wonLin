<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>wonLin</title>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha2/dist/css/bootstrap.min.css" rel="stylesheet"
            integrity="sha384-aFq/bzH65dt+w6FI2ooMVUpc+21e0SRygnTpmBvdBgSdnuTN7QbdgL+OapgHtvPp" crossorigin="anonymous">
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.3/font/bootstrap-icons.css">
        <link rel="stylesheet" href="./style.css">
    </head>
    <body>
        <form method="post" action="/searched">
            <div class="input-group mt-3 p-3">
                <input type="search" class="form-control inputKey" placeholder="Busc..." name="gifSearched">
                <button class="btn btn-primary s-G">Buscar</button>
            </div>
        </form>
        <h4 class="text-center">Da una bienvenida con un divertido GIF</h4>
        <h6 class="text-danger text-center">
            Cuando seleccione un gif, no olvide refrescar la página de su perfil...Gracias
        </h6>
        <div class="gif">
            <% if(typeof trendingData !=="undefined" ){ %>
                <% for(let urlSearched = 0; urlSearched < trendingData.data.length;urlSearched++){ %>
                    <img src=<%=trendingData.data[urlSearched].images.downsized.url%> 
                    alt="<%= trendingData.data[urlSearched].title %>"/>
                <% } %>
            <%} %>
            <% if(typeof trending !=="undefined" ){ %>
                <% for(let url=0; url<trending.data.length;url++){ %>
                    <img src=<%=trending.data[url].images.downsized.url%> 
                    alt="<%= trending.data[url].title %>"/>
                <% } %>
            <%} %>
            
        </div>
        <script src="./Presentation.js" type="module"></script>
    </body>

</html>