<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>wonLin</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-aFq/bzH65dt+w6FI2ooMVUpc+21e0SRygnTpmBvdBgSdnuTN7QbdgL+OapgHtvPp" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.3/font/bootstrap-icons.css">
    <link href="/style.css" rel="stylesheet" crossorigin="anonymous">
    <link href="/index.css" rel="stylesheet" crossorigin="anonymous">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Bagel+Fat+One&display=swap" rel="stylesheet">
</head>
<body style="background: rgb(9, 47, 69);">
    <form method="post" action="/recUser">
        <div class="card border border-0" style="margin-top: 50px; background: none;">
            <div class="card-header border border-0">
                <svg height="50">
                    <circle cx="240" cy="20" r="20" fill="rgb(0,194,168)"></circle>
                    <ellipse class="ellip" cx="230" cy="15" rx="10" ry="5" fill="white"></ellipse>
                    <ellipse cx="250" cy="15" rx="10" ry="5" fill="white"></ellipse>
                    <circle class="eyeLeft" cx="230" cy="15" r="3" fill="black"></circle>
                    <circle class="eyeRight" cx="250" cy="15" r="3" fill="black"></circle>
                </svg>
            </div>
            <div class="card-body border border-0 text-white">
                <label for="email">Correo</label>
                <input type="email" class="form-control p-2 border border-primary mt-3" id="email" 
                name="emailUser" required />
                <label class="mt-3" for="Password">Contraseña</label>
                <input type="password" class="form-control p-2 border border-primary mt-3" id="Password" name="passwd"
                    required />
                <div class="d-flex justify-content-center mt-4">
                    <button class="btn text-white" type="submit"
                        style="background: linear-gradient(to right, rgb(0,194,168), blue);">
                        Enviar
                    </button>
                </div>
                <div class="d-flex justify-content-center" style="margin-top: 40px;">
                    <button class="btn btn-warning">
                        <a class="nav-link" href="/new">Registrarse</a>
                    </button>
                </div>
            </div>
        </div>
    </form>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script>
        const form = document.querySelector("form")
            form.addEventListener("formdata",(event)=>{
                event.stopPropagation()
                alert("😭 No olvide de recargar la página después de iniciar sesión---Gracias...")
            },{once:true})
    </script>
    <% if(typeof alert !="undefined" ){ %>
        <script>
            Swal.fire({
                icon: `<%= iconAlert %>`,
                title: `<%= titleAlert %>`,
                text: `<%= textAlert %>`
            })
        </script>
    <% } %>
</body>
</html>